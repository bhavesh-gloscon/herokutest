<h1>Listing gloscons</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gloscons.each do |gloscon| %>
  <tr>
    <td><%= gloscon.name %></td>
    <td><%= gloscon.address %></td>
    <td><%= gloscon.email %></td>
    <td><%= link_to 'Show', gloscon %></td>
    <td><%= link_to 'Edit', edit_gloscon_path(gloscon) %></td>
    <td><%= link_to 'Destroy', gloscon, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gloscon', new_gloscon_path %>
